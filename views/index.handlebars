<div class="jumbotron jumbotron-fluid palegreen-bg text-white p-0 m-0">
  
  <h1 class="text-center display-4 p-0 m-0">
    <span class="d-none d-md-inline">EAT-DA-</span>BURGERS <span class="badge badge-pill red-bg m-0"><i class="fas fa-italic"></i><i class="fas fa-italic"></i></span>
  </h1>
  <div class="text-center">
        <!--  Modal trigger button --> 
    <button type="button" class="btn btn-outline-light" data-toggle="modal" data-target="#burgerModal">
      <i class="fas fa-hamburger"> Add New Burger</i>
    </button>
  </div>

</div>
  <!-- bootstrap container for contents -->
<div class="container-fluid font-weight-bold text-center">
  
  <div class="row text-white">
  <!-- Uneaten Burger Row -->
    <div class="row col-md-6">
      {{>uneaten}}
    </div>
  <!-- ate Burger Row -->
    <div class="row col-md-6">
      {{>ate}}
    </div>

  </div>
  {{>createmodal}}
	{{>devourmodal}}
</div>